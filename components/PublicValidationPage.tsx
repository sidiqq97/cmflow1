'use client';

import React from 'react';
import PublicValidationPage from '../src/app/approve/[token]/page';

export const ValidationPortal: React.FC = () => {
  return <PublicValidationPage />;
};

export default ValidationPortal;
